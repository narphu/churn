apiVersion: serving.kserve.io/v1beta1
kind: InferenceService
metadata:
  name: churn-rf
  namespace: default
spec:
  predictor:
    sklearn:
      storageUri: "pvc://model-pvc/sklearn-model"
    resources:
      limits:
        cpu: 1
        memory: 1Gi
